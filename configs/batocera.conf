# Batocera SNES Configuration

# Set SNES as the default system at startup
system.default=snes

# SNES emulator: snes9x via libretro
snes.emulator=libretro
snes.core=snes9x

# UI kiosk mode (hides settings menus from end users)
ui.mode=kiosk
